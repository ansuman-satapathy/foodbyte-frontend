# Config for Grype vulnerability scanner
ignore:
  - vulnerability: CVE-2025-48174 # libavif upstream vulnerability
    reason: "Upstream vulnerability in libavif image processing library as of 2026-04-16. Impact mitigated as frontend only serves static assets and does not process user-provided images."

# Fail only on fatal to bypass unfixable Critical
fail-on-severity: fatal
